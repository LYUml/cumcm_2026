# 问题二：预测—优化—仿真一体化模型

## 1. 模型选择原则

在每天 0:00，只使用该时刻以前的历史负荷和光伏数据预测当天 144 个十分钟时段的净负荷

\[
N_{d,t}=L_{d,t}-P_{d,t}.
\]

所有候选方法共用同一费用结算器。使用 2025-02-01 至 2025-08-31 作为模型选择期；2025-09-01 至 2025-12-31 作为不参与选模的最终测试期，避免用同一批数据选模和报告性能。选模指标是实际结算费用，而不是仅看 MAE 或 RMSE。

## 2. 候选预测方法

1. `seasonal_naive_7d`：前一周同一时刻；
2. `weekday_mean_4w`：过去四个同星期日、同一时刻的均值；
3. `recent_median_7d`：过去七天同一时刻的中位数；
4. `ridge_expanding`：使用日内周期、年周期、周末标记以及 1/7/14/21/28 日滞后的滚动岭回归；
5. `cost_aware_q80`：以前一周预测为中心，用过去至多 56 天的因果残差估计条件 0.8 分位数。
6. `scenario_recent_6/12/20`：将最近历史整日残差轨迹构造为 6、12 或 20 条净负荷场景，并把正常购电费与场景期望紧急购电费放入同一个两阶段随机线性规划；场景数仅在模型选择期确定。
7. `scenario_knn_12`：根据预测日已知的前一周净负荷轮廓，在历史中寻找形状最相似的 12 个先例，并使用这些先例当时产生的完整误差轨迹。
8. `scenario_reduced_12`：从最近 56 天的完整误差轨迹池中用快速前向式场景缩减选择 12 个代表轨迹，再将被删除轨迹的概率质量转移到最近代表轨迹。

紧急购电价格为正常价格的 5 倍。忽略跨时段耦合时，少计划 1 kWh 相比提前购买增加约 \(4p_t\) 的损失，多计划 1 kWh 的损失为 \(p_t\)，故报童模型临界分位数为

\[
\tau^*=\frac{4p_t}{4p_t+p_t}=0.8.
\]

## 3. 日前调度模型

对每个十分钟时段定义计划购电量 \(G_t\)、充电量 \(C_t\)、放电量 \(D_t\)、弃电量 \(W_t\) 和期末电量 \(S_t\)。目标为

\[
\min \sum_{t=1}^{144}p_tG_t.
\]

预测能量平衡为

\[
G_t+D_t-C_t-W_t=\widehat N_t\Delta t,
\qquad \Delta t=1/6.
\]

储能状态满足

\[
S_t=S_{t-1}+0.9C_t-D_t/0.9,
\]

\[
1200\le S_t\le10800,
\qquad 0\le C_t,D_t\le5000/6.
\]

为消除单日优化的期末透支效应，基准模型采用循环终端约束 \(S_{144}=S_0=6000\)。该假设应在论文中明确，并在敏感性实验中与期末储能下界或终端价值函数比较。

## 4. 真实运行与费用结算

计划确定后，使用当天真实净负荷计算不平衡量：

\[
\delta_t=N_t\Delta t-(G_t+D_t-C_t).
\]

紧急购电和剩余电量分别为

\[
E_t^{\mathrm{emg}}=\max(\delta_t,0),
\qquad W_t^{\mathrm{real}}=\max(-\delta_t,0).
\]

实际总费用为

\[
J=\sum_tp_tG_t+\sum_t5p_tE_t^{\mathrm{emg}}.
\]

## 5. 复现

在项目根目录运行：

```bash
python3 q2_model.py
python3 q2_validate.py
```

结果保存在 `q2_outputs/`。`model_comparison.csv` 保存完整模型比较，`result2.xlsx` 为按最佳模型填写的提交文件，两个 JSON 文件保存物理约束、数据泄漏、完全信息下界和 Excel 对账检查。
